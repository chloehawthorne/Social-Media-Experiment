<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Octo_Social_Media.Views.SearchPage">
    <ContentPage.Content>

        <StackLayout
            Style="{StaticResource StackLayoutCaitlin}">

            <Grid
                Style="{StaticResource GridStylingCaitlin}">

                <Label
                    Text="Search"
                    Style="{StaticResource HeaderLabelCaitlin}" />

                <Label
                    Text="What would you like to search?"
                    Style="{StaticResource SearchLabelsCaitlin}"
                    Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="2" />

                <Entry
                    Text="Please type here"
                    Style="{StaticResource EntryStyleCaitlin}" 
                    Grid.Row="3"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"/>

            </Grid>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>